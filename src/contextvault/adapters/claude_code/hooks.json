{
  "_comment": "ContextVault hooks for Claude Code. Installed by `contextvault adapter add claude-code`, which merges these entries into ~/.claude/settings.json (preserving user-added hooks and replacing any prior contextvault entries).",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "command": "contextvault hot 2>/dev/null || true"
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "contextvault capture --cwd \"${CLAUDE_PROJECT_DIR:-$PWD}\" --mode incremental >/dev/null 2>&1 &"
          }
        ]
      }
    ]
  }
}
